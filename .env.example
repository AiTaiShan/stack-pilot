# Database
DATABASE_URL=postgresql://user:password@localhost:5432/stackpilot
REDIS_URL=redis://localhost:6379
POSTGRES_DB=stackpilot
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# CORS (JSON 数组格式)
CORS_ORIGINS=["http://localhost:5173"]

# JWT
# 注意：生产环境务必使用随机生成的强密钥
JWT_SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=120
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# LLM
# 支持 openai / dashscope（阿里百炼）等 OpenAI 兼容接口
LLM_PROVIDER=dashscope
LLM_API_KEY=your-api-key-here
LLM_MODEL=qwen-plus
# 阿里百炼兼容接口地址，使用 OpenAI 时可留空
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Application
APP_NAME=StackPilot
APP_VERSION=1.0.0
# 注意：生产环境务必设置为 false
DEBUG=true

# Logging
# 日志级别: trace / debug / info / warn / error
LOG_LEVEL=info
# 日志输出目录（相对于项目根目录）
LOG_DIR=logs
# Rust 后端日志级别（覆盖 LOG_LEVEL，用于 tracing env-filter）
RUST_LOG=stackpilot_backend=info,tower_http=info
